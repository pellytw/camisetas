<%= form_for(@detalle_compra) do |f| %>
  <% if @detalle_compra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detalle_compra.errors.count, "error") %> prohibited this detalle_compra from being saved:</h2>

      <ul>
      <% @detalle_compra.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cantidad %><br />
    <%= f.number_field :cantidad %>
  </div>
  <div class="field">
    <%= f.label :precio_unitario %><br />
    <%= f.text_field :precio_unitario %>
  </div>
  <div class="field">
    <%= f.label :producto_id %><br />
    <%= f.number_field :producto_id %>
  </div>
  <div class="field">
    <%= f.label :compra_id %><br />
    <%= f.number_field :compra_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
