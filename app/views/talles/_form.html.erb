<%= form_for(@talle) do |f| %>
  <% if @talle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@talle.errors.count, "error") %> prohibited this talle from being saved:</h2>

      <ul>
      <% @talle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
